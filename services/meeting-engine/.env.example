# meeting-engine — caminho quente do tempo real
PORT=8080

# Segredo compartilhado com apps/web para assinar o JWT do WS (?token=)
# Gerar: openssl rand -hex 32
ENGINE_WS_SECRET=

# Supabase (service_role — NUNCA expor no client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# F3+: pipeline de IA
ANTHROPIC_API_KEY=
